#ifndef librarySection10
#define librarySection10

extern int a; //this variable should be in another  place defineded
                   //external (outside) it means that here we do not reserve MEMORY for variable a
void showHelp();
#endif